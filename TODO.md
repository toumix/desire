# TODO

USER, [memory#63](https://github.com/toumix/memory/pull/63), 2026-08-26, verbatim:

> no I don't want to close these four PRs I'm fine with keeping them thanks, in fact Tommaso started doing some experiments with them already

> I've replied to #609 already how come you didn't see it? seems like the sweep doesn't work so well with issues?

> This PR description is getting stale quite quick after a day, I think we're gonna go back to one PR per day
> Also it seems like Evening is the talkative one, it would make sense to let it only write the turn files with no PR description and Birdsong would be in charge of filling that in the most concise way possible, tables etc should go to the readme there's no point repeating them here

USER, live session, 2026-08-26, verbatim:

> yeah i just merged the current one so that's fine, go ahead with the rest and i'll give my feedback on the desire PR directly

## Work

- [ ] file the two rulings as open issues on DESIRE_REPO, closed by this PR
- [ ] `sweep.py`: an unanswered USER comment is a condition, not an event — drop `>= since` from both unanswered branches
- [ ] `AGENTS.md`: one memory PR per day, not one at a time
- [ ] `AGENTS.md`: the memory PR template stops carrying the queue as a table
- [ ] `EVENING.md`: Evening writes its turn file and a PR comment, never the description
- [ ] `BIRDSONG.md`: Birdsong owns the description, as concise as it can be, tables live in the board
- [ ] `CHANGELOG.md` entry for both
