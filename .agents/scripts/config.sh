#!/usr/bin/env bash
# The config, and the only copy of it: AGENTS.md imports this file, the scripts source it.
# USER is USER_LOGIN here because $USER is the shell's own.

export USER_LOGIN="toumix"
export AGENT="toumix-agents"
export AGENT_EMAIL="agents@toumi.email"
export WORK_REPOS="discopy/discopy"
export MEMORY_REPO="toumix/memory"
export DESIRE_REPO="toumix/desire"
export APPROVE_EMOJI="rocket"
